/* 
    из переменной #"путь", в которой содержится таблица с путями,
    извлекаем путь к папке с дополнитлеьными часами
 */
let
    source = #"путь"{[файл = "папка с доп часами"]}[путь]
in
    source